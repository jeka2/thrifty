<%= form_tag '/add_to_cart', id: 'cart-form', method: 'post', remote: true do %>
    <%= hidden_field_tag :item_id, item_id %>
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= submit_tag 'Add To Cart', id:'cart-button', class:['btn', 'btn-success'] %>
<% end %>