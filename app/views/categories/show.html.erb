Name: <%= @category.name %><br>
Description: <%= @category.description %><br>
<%= render partial: 'items/single', collection: @items, as: :item %>
<%= paginate_buttons(@items, @per_page, @page, {'category': @category.id } ) %>