Description: <%= @category.description %><br>
<% @items.each do |item| %>
    <%= link_to item.name, item_path(item) %><br>
    <%= item.description.truncate(50) %><br>
<% end %>
<%= paginate_buttons(@category, @per_page, @page) %>