Department Name: <%= @department.name %><br>
<%= render partial: 'items/single', collection: @items, as: :item %>
<%= paginate_buttons(@all_items, @per_page, @page, { 'department': @department.id }) %>